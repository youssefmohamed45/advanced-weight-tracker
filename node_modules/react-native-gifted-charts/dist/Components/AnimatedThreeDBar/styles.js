import { StyleSheet } from 'react-native';
export var styles = StyleSheet.create({
    container: {
        width: '150%',
        position: 'absolute',
        bottom: 0,
        overflow: 'hidden',
    },
    row: {
        flexDirection: 'row',
    },
});
